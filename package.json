{
  "name": "vt-parser",
  "version": "0.0.1",
  "description": "Virtual-Template DSL",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "npm test",
    "dev": "NODE_ENV=dev nodemon --exec mocha",
    "build": "browserify build.js -o build/virtual-template.js && uglifyjs build/virtual-template.js > build/virtual-template.min.js"
  },
  "dependencies": {
    "simple-virtual-dom": "^0.1.8"
  },
  "author": "Livoras",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "cheerio": "^0.19.0",
    "jsdom": "^8.0.1"
  }
}
